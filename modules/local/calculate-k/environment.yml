name: calculate-k
channels:
  - conda-forge
  - nodefaults
dependencies:
  # R interpreter
  # also provides the stats functions used to find the elbow of the coherence curve
  # (`smooth.spline`, `predict`) and to adjust the permutation p-values (`p.adjust`)
  - r-base=4.5

  # packages used in module
  - r-tidyverse
  - r-glue
